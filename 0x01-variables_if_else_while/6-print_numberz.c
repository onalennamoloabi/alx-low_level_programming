#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints digits of base 10 from 0
 * Return: 0 (SUCCESS)
 */
int main(void)
{
	int digit;

	for (digit = '0'; digit <= 0; digit++)

		putchar(digit);
		putchar('\n');
		return (0);
}
